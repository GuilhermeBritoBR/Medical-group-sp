const validação = require('./validation');

function cadastro(){
    window.alert("teste");
    return validação;
    app.get('/', (req, res) => {
    db.query("INSERT INTO medical (user, senha) VALUES (username, passoword) ", (err,result) => {
      res.send(result);
  
  
    })
   })
    
    
  }
  module.exports = cadastro;